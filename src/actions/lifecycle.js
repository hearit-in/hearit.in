import { createAction } from 'redux-actions';

export const SET_HAS_LOADED_PERSISTENT_STATE = "SET_HAS_LOADED_PERSISTENT_STATE";
export const setHasLoadedPersistentState = createAction(SET_HAS_LOADED_PERSISTENT_STATE);
